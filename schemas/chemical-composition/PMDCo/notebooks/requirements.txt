# Transformation
jsonata-python>=0.6.0   # JSON transformation engine (import jsonata)

# RDF conversion and SHACL validation
pyyaml>=6.0      # load JSON-LD context from schema.oold.yaml
rdflib>=7.0.0    # native JSON-LD 1.1 parser; handles @prefix: true and CURIE @id values
pyshacl>=0.25.0

# Jupyter kernel registration (needed when JupyterLab is installed outside the venv)
ipykernel>=6.0.0

# Include jupyterlab here only if you are NOT using a global/pipx install.
# jupyterlab>=4.0.0
